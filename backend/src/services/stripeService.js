export async function createStripePaymentIntent(amount, currency = 'usd') {
  // your code to create payment intent
}

export async function confirmStripePayment(paymentIntentId) {
  // your code to confirm payment intent
}
